dist: trusty
language: python
before_install:
  - sudo apt-get update -qq
install:
  - pip install --upgrade pip
  - pip install tox
  - pip install --upgrade flake8
  - pip install six
script:
  - bash test.sh

